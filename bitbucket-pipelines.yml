image: cgswong/aws

pipelines:
  branches:
    master:
      - step:
          name: Deploy to test
          deployment: test
          # trigger: manual  # Uncomment to make this a manual deployment.
          script:
            - aws s3 --region "us-east-1" sync prebid/ s3://adysis-ad-delivery-js --cache-control "public, max-age=14400" --delete          